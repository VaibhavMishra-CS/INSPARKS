// theme.js — deprecated.
// Dark mode and the theme toggle have been removed from INSPARKS.
// The app now uses a single fixed white/blue theme (see theme.css).
// This file is kept only so any stray legacy import doesn't crash the build;

export {};